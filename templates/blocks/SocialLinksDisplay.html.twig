{# Pointer block kind ("social_links_display"): holds no data of its own, always renders the
   site-wide "social_links" singleton (see SocialLinksCrudController) - lets editors drop it into
   any page's block flow without re-defining the links #}
<twig:c975LSocial:SocialLinks/>
