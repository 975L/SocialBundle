{# <twig:c975LSocial:SocialLinks/> #}
{# Renders the first "social_links" block found anywhere, so no singleton entity is needed #}
{% set block = social_link_block() %}
{% if block is not null %}
    {{ render_block(block) }}
{% endif %}
